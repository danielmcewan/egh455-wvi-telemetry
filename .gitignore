# Virtual environment
.venv/
__pycache__/
*.py[cod]

# Runtime telemetry log - regenerated on every run, not source
data/*.db
data/*.db-shm
data/*.db-wal

# Captured target snapshots
targets/

.env

.vercel

# Scratch image the publisher stub demo regenerates
_demo_target_crop.jpg

# Local editor/agent tooling config. Machine-specific - launch.json here
# hardcodes a macOS venv path, and settings.local.json would carry personal
# permission allowlists - so it is not shared with the team.
.claude/
